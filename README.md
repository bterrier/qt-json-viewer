Qt JSON Viewer

This is a small JSON viewing app.
It reads JSON files and display the JSON in a tree widget.

The core of the app is the JsonModel class (jsonmodel.h and jsonmodel.cpp) that can be taken and included in other projects.